# Dev always uses the Vite dev server URL + proxy; VITE_SOCKET_URL is ignored in dev.
# Set for production builds / vite preview when the API is on another host:
# VITE_SOCKET_URL=http://localhost:3001
