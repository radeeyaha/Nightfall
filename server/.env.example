PORT=3001
# Optional comma-separated list. If unset: dev reflects any Origin (5173, 5175, …);
# production uses a small localhost default — set this to your real site in deploy.
# CLIENT_ORIGIN=https://app.example.com
